
  # OmniCase

  This is a code bundle for OmniCase. The original project is available at https://www.figma.com/design/2ZOf9rDgaTcxWeEAiE0j7C/OmniCase.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  - using mdx eduroam wifi would have problem
  - run `npm i `& `npm run dev` both on ./evidence-app and ./evidence/server
  